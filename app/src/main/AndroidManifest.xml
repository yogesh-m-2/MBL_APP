<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MBLFoods"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity">
            <!-- Optionally, you can specify intent filters or other attributes here -->
        </activity>
        <activity android:name=".OutletViewActivity">
            <!-- Optionally, you can specify intent filters or other attributes here -->
        </activity>
        <activity android:name=".InvoiceActivity">
            <!-- Optionally, you can specify intent filters or other attributes here -->
        </activity>
        <activity android:name=".SignatureActivity">
            <!-- Optionally, you can specify intent filters or other attributes here -->
        </activity>
    </application>
    <meta-data
        android:name="com.google.android.gms.auth.GOOGLE_SIGN_IN_CLIENT_ID"
        android:value="1073577142554-2l0kams516jpckvhhvgiaa6vs5626jme.apps.googleusercontent.com" />

</manifest>